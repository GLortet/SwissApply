services:
  web:
    build: .
    environment:
      PORT: 3000
      SWISSAPPLY_STORAGE_DIR: /app/storage/private
    ports: ["3000:3000"]
    volumes: ["swissapply-data:/app/storage"]
volumes:
  swissapply-data:
